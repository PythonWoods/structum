[build-system]
requires = ["hatchling>=1.24"]
build-backend = "hatchling.build"

[project]
name = "structum"
version = "0.0.1"
description = "Structum — Enterprise Code Structure & Documentation Engine (prerelease skeleton)"
readme = "README.md"
license = { text = "Apache-2.0" }
authors = [
  { name="PythonWoods" }
]
requires-python = ">=3.11"
dependencies = []

[tool.hatch.build.targets.wheel]
packages = ["src/structum"]
